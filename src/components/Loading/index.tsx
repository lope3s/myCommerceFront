import React from "react";
import { Container, Loading } from "./styles";

export const LoadingPage: React.FC = () => {
    return (
        <Container>
            <Loading />
        </Container>
    );
};
